Import( 'project', 'libs' )

project.StaticLibrary(
		'reportGenerator',
		dirs      = ['src'],
		includes  = ['src'],
		libraries = [
			libs.common,
			libs.basicElement,
			],
		shared = True
	)


