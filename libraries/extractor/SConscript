Import( 'project', 'libs' )

project.StaticLibrary(
	'extractor',
	dirs      = ['src'],
	includes  = ['src'],
	libraries = [
		libs.boost_filesystem,
		libs.common,
		libs.report,
		],
	shared = True
)
