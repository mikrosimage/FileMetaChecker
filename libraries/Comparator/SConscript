Import( 'project', 'libs' )

project.StaticLibrary(
		'comparator',
		dirs      = ['src'],
		includes  = ['src'],
		libraries = [
			libs.common,
			libs.fileReader,
			libs.specReader,
			libs.basicElement,
			libs.reportGenerator,
			],
		shared = True
	)


